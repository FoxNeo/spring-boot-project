# REST endpoint HTTP POTS Spring-boot application

This is a simple project, it can be used as a template for a huge project.


Run with other profile
======================
```
mvn spring-boot:run
```
```javascript
{ 
    "name": "Test",
    "chapterCount": 5
}
```
# Convert spring-boot application to WAR Web Application

This is a simple project, it can be used as a template for a huge project. Ready to deploy on Tomcat server

## Build
```
$ mvn clean install
```